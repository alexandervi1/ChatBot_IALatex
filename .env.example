# ================================
# Chatbot IA Premium V4.1 - Variables de Entorno
# ================================
# Copia este archivo como .env y configura los valores

# --- Base de Datos PostgreSQL ---
POSTGRES_USER=postgres
POSTGRES_PASSWORD=tu_password_seguro
POSTGRES_DB=chatbot_db

# --- Connection Pool (Opcional) ---
# DB_POOL_SIZE=10
# DB_MAX_OVERFLOW=20
# DB_POOL_TIMEOUT=30
# DB_POOL_RECYCLE=1800
# DB_ECHO=false

# --- API de Google Gemini ---
# Obtén tu clave en: https://aistudio.google.com/app/apikey
GEMINI_API_KEY=tu_api_key_de_google

# --- Seguridad JWT (OBLIGATORIO) ---
# Genera una clave segura con: python -c "import secrets; print(secrets.token_hex(32))"
JWT_SECRET_KEY=genera_una_clave_segura_de_al_menos_32_caracteres

# --- Encriptación de API Keys (OBLIGATORIO) ---
# Genera una clave segura con: python -c "import secrets; print(secrets.token_urlsafe(32))"
ENCRYPTION_KEY=genera_una_clave_segura_para_encriptacion
# ENCRYPTION_SALT=opcional_salt_personalizado

# --- Frontend (Producción) ---
# URL del backend para el frontend en producción
# NEXT_PUBLIC_API_URL=https://tu-dominio.com/api

# --- CORS Origins (Opcional) ---
# Orígenes permitidos separados por coma
# CORS_ORIGINS=http://localhost:3000,https://tu-dominio.com
